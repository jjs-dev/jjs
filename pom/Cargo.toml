[package]
name = "pom"
version = "0.1.0"
authors = ["Mikail Bagishov <bagishov.mikail@yandex.ru>"]
edition = "2018"
description = "problems&contests object model (shared between invoker and tt)"

[dependencies]
serde = { version = "1.0.97", features = ["derive"] }
