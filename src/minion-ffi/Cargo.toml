[package]
name = "minion-ffi"
version = "0.1.0"
authors = ["Mikail Bagishov <bagishov.mikail@yandex.ru>"]
edition = "2018"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
minion = {path = "../minion"}

[build-dependencies]
cbindgen = { version = "0.12.0", default-features = false }
